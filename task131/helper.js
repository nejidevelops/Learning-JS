export const printArray = (arr) => {
  // for (let i = 0; i < arr.length; i++) {
  //   console.log(arr);
  // }
  for(var i = 0; i < arr.length ; i++){
    console.log(arr[i]);
  }
  // console.log(arr.join() );
}
